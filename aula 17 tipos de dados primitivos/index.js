// aqui temos string, number, undefiend(quando não coloca nada um valor na variavel), null, boolean
const nome ="Luiz"; //string //
const num1 = 10;//number //
let nomeAluno; // undefined não aponta pra local nenhuma na memória
const sobrenomeAluno = null;  // não aponta pra local nenhuma na memória //
const aprovado = false; // Boolean = true, false //

console.log(typeof nome, num1, sobrenomeAluno); // serve para ver qual é o tipo//